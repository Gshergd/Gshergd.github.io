# Shared site components

Place navigation, footer, modal, buttons, and other components used by multiple character archives here.
Keep character-only visuals and copy inside that character's `features/` folder.
